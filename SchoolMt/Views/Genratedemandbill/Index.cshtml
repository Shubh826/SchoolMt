@model MDL.GenrateDmdbillMdl
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


@*@using (Html.BeginForm("Genratedemandbill", "Genratedemandbill"))
    {*@
@using (Html.BeginForm("Genratedemandbill", "Genratedemandbill", FormMethod.Post, new { enctype = "multipart/form-data", id = "DemandBillForm" }))
{
    @*@Html.HiddenFor(E => E.PK_RoleId)*@
    <div class="page-heading mb-0">
        <div class="page-title">
            <div class="row">
                <div class="col-12 col-md-6 order-md-1 order-last">
                    <h3>Demand Bill</h3>
                </div>
                <div class="col-12 col-md-6 order-md-2 order-first">
                    <nav aria-label="breadcrumb" class="breadcrumb-header float-start float-lg-end">
                        <ol class="breadcrumb">
                            <li class="breadcrumb-item"><a href="">Dashboard</a></li>
                            <li class="breadcrumb-item active" aria-current="page">Demand Bill</li>
                        </ol>
                    </nav>
                </div>
            </div>
        </div>
    </div>
    <div class="card">
        <div class="card-content">
            <div class="card-body">
                <div class="row">
                    <div class="col-lg-8 offset-lg-1">
                        @*<form class="form form-horizontal">*@
                        <div class="form-body">
                            <div class="row">
                                @*@Html.ValidationSummary(true, "", new { @class = "text-danger" })*@
                                @*<div class="col-md-3">
                                        <label class="setForm">Role For</label>
                                    </div>
                                    <div class="col-md-7 form-group">
                                        <ul class="list-unstyled mb-0">
                                            <li class="d-inline-block me-2 mb-1">
                                                <div class="form-check">
                                                    <div class="checkbox">
                                                        @Html.CheckBoxFor(E => E.IsCompany, new { @class = "form-check-input CompanyClass" })
                                                        <label for="checkbox11">Company</label>
                                                    </div>
                                                </div>
                                            </li>
                                            <li class="d-inline-block me-2 mb-1">
                                                <div class="form-check">
                                                    <div class="checkbox">
                                                        @Html.CheckBoxFor(E => E.IsClient, new { @class = "form-check-input ClientClass" })
                                                        <label for="checkbox12">Client</label>
                                                    </div>
                                                </div>
                                            </li>
                                        </ul>
                                    </div>*@
                                <div class="col-md-3 ClientDiv">
                                    <label class="setForm bold">School Name<span style="color:red">*</span></label>
                                </div>
                                <div class="col-md-7 form-group ClientDiv">
                                    @Html.DropDownListFor(E => E.FK_CompanyId, new SelectList((System.Collections.IEnumerable)ViewData["CompanyList"], "ID", "Value"), "--Select--", new { @class = "form-control select2" })
                                    @*@Html.ValidationMessageFor(E => E.FK_CompanyId, "", new { @class = "redText", @style = "color:red" })*@
                                </div>
                                <div class="col-md-3">
                                    <label class="setForm  bold">Class Name<span style="color:red">*</span></label>
                                </div>
                                <div class="col-md-7 form-group">
                                    @Html.DropDownListFor(E => E.ClassName, new SelectList((System.Collections.IEnumerable)ViewData["ClassList"], "Value", "Value"), "--Select--", new { @class = "form-control select2" })
                                    @*@Html.ValidationMessageFor(E => E.ClassName, "", new { @class = "redText", @style = "color:red" })*@
                                </div>


                                <div class="col-12 text-center mt-4">
                                    <button type="submit" class="btn btn-primary me-2">Submit</button>
                                    <a href="@Url.Action("Index")" class="btn btn-danger">Cancel</a>
                                </div>

                            </div>
                        </div>
                        @*</form>*@
                    </div>
                </div>
            </div>
        </div>
    </div>
}

@*@*******START Loader******@
<div class="modalBgLoader" style="display: none"></div>
<div class="modalLoaderCenter" style="display: none">
    <img alt="Loading..." src="~/App_Images/loader.gif" />
    <span>Loading...</span>
</div>
@*@*******END Loader******@

@section scripts {
    @Scripts.Render("~/bundles/jqueryval")
    <link href="~/app-assets/vendors/css/forms/selects/select2.min.css" rel="stylesheet" />
    <script src="~/app-assets/vendors/js/forms/select/select2.full.min.js"></script>

    <script type="text/javascript">
        $(document).ready(function (e) {
            $('.select2').select2();

            $('#DemandBillForm').on('submit', function (e) {
                e.preventDefault(); // prevent full form submission

                if ($("#FK_CompanyId").val() === "" || $("#FK_CompanyId").val() === "--Select--" || $("#FK_CompanyId").val()==null) {
                    alert("Please Select School.");
                    e.preventDefault();
                    return false;
                }
                if ($("#ClassName").val() === "" || $("#ClassName").val() === "--Select--" || $("#ClassName").val() == null) {
                    alert("Please Select Class.");
                    e.preventDefault();
                    return false;
                }

                var form = $('#DemandBillForm')[0];
                var formData = new FormData(form); // support for file upload

                $.ajax({
                    url: '@Url.Action("Genratedemandbill", "Genratedemandbill")',
                    type: 'POST',
                    data: formData,
                    beforeSend: function () {
                        $(".modalBgLoader, .modalLoaderCenter").show();
                    },
                    complete: function () {
                        $(".modalBgLoader, .modalLoaderCenter").hide();
                    },
                    processData: false,
                    contentType: false,
                    success: function (response) {
                        // success response from controller
                        if (response === 1) {
                            window.location = '@Url.Action("CreatePdf", "Genratedemandbill")';
                        }
                        else {
                            // window.location = '@Url.Action("CreatePdf", "Genratedemandbill")';

                            alert("Data Not Found.");
                            e.preventDefault();
                            return false;
                        }
                    },
                    error: function () {
                        alert("Data Not Found.");
                        e.preventDefault();
                        return false;
                        alert('Error while submitting the form.');
                    }
                });

            });


            //if ($('#IsClient').is(':checked')) {
            //    $('.ClientDiv').show();
            //}
            //else {
            //    $(".ClientDiv").hide();

            //}
            //if ($('#IsClient').is(':checked')) {
            //    $('#IsClient').prop('checked', true);
            //    $('#IsCompany').prop('checked', false);
            //}
            //if ($('#IsCompany').is(':checked')) {
            //    $('#IsClient').prop('checked', false);
            //    $('#IsCompany').prop('checked', true);
            //}
            //$(".CompanyClass").click(function () {
            //    $('#IsClient').prop('checked', false);
            //    $('#IsCompany').prop('checked', true);
            //    $(".ClientDiv").hide();
            //});
            //$(".ClientClass").click(function () {
            //    debugger
            //    $('#IsCompany').prop('checked', false); //uncheck checkboxe
            //    $('#IsClient').prop('checked', true);  //check the clicked one
            //    $('.ClientDiv').show();
            //});

        });


    </script>
}